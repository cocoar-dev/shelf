services:
  shelf:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - /srv/docs:/data/docs:ro
    environment:
      - Shelf__DocsRoot=/data/docs
    restart: unless-stopped
